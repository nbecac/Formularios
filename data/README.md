# Base de datos

En esta carpeta se creará automáticamente la base de datos `formularios_agent.db` cuando se ejecute el script `setup.bat` o se inicie el backend.
No subir este archivo a control de versiones (está en `.gitignore`).
